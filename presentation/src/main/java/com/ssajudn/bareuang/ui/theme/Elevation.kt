package com.ssajudn.bareuang.ui.theme

import androidx.compose.ui.unit.dp

/** Small, named elevation roles. Shadows communicate hierarchy, not decoration. */
object AppElevation {
    /** The receipt preview lifts slightly to read as a physical paper artifact. */
    val ReceiptPaper = 4.dp
}
