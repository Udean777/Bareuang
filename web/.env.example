# Copy to .env.local for `vercel dev` (web/.env.local, not committed)
# Get key at https://aistudio.google.com → Create API key
GEMINI_API_KEY=AIza...
# Optional — rate limit per IP per 24h (default 20)
RATE_LIMIT_RPD=20
